# timezone configuration (optional, defaults to UTC)
# Common values: America/New_York, America/Los_Angeles, Europe/London, UTC
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# TZ=UTC

# storage configuration (optional, defaults to /data/cameras)
# STORAGE_PATH=/data/cameras

# authentication configuration (optional, defaults to /opt/tapes/passwords)
# PASSWORDS_PATH=/opt/tapes/passwords

# JWT_KEY is REQUIRED - used to sign authentication tokens
# Generate a secure random key with: openssl rand -hex 32
JWT_KEY=

# jwt issuer (optional, defaults to 'tapes')
# Identifies the application that issued the token
# JWT_ISSUER=tapes

# debug logging (optional, defaults to false)
# set to true, 1, or yes to enable debug-level logging
# info and error messages are always displayed
# DEBUG=false

# retention configuration (optional, defaults to 90 days)
# number of days to keep recordings before automatic deletion
# set to 0 to disable automatic deletion and keep all recordings
# NOTE: MediaMTX also has its own retention setting in mediamtx.yml (recordDeleteAfter)
# Both systems can run cleanup independently - set both to the same value
# RETENTION_DAYS=90
